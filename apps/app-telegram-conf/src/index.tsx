import React from 'react';
import ReactDOM from 'react-dom/client';

const root = ReactDOM.createRoot(document.getElementById('root')!);
// const AuthenticationAppRemote = React.lazy(() => import("account/App"));



root.render(
  <React.StrictMode>
    hii
  </React.StrictMode>
);

