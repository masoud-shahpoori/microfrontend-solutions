// You can write your own logic here to determine the actual url
import config from "../../../config";

//@ts-ignore
window.authentication_url = config.apps.authentication.url

import("./bootstrap");
